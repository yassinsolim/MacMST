cmake_minimum_required(VERSION 3.20)
project(MacMST VERSION 0.1.0 LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
option(MACMST_ENABLE_SANITIZERS "Enable address and undefined-behavior sanitizers" OFF)
option(MACMST_ENABLE_HARDWARE_TESTS "Register opt-in public read-only macOS hardware tests" OFF)
option(MACMST_ENABLE_DPDV_OPEN_EXPERIMENT "Build the explicitly authorized isolated DPDV open/close tools" OFF)

add_library(macmst_options INTERFACE)
if(CMAKE_CXX_COMPILER_ID MATCHES "Clang|GNU")
    target_compile_options(macmst_options INTERFACE
        -Wall -Wextra -Wpedantic -Wconversion -Wsign-conversion -Werror)
    if(MACMST_ENABLE_SANITIZERS)
        target_compile_options(macmst_options INTERFACE -fsanitize=address,undefined -fno-omit-frame-pointer)
        target_link_options(macmst_options INTERFACE -fsanitize=address,undefined)
    endif()
endif()

include(CTest)
add_library(macmst_displayport src/displayport/dpcd/capabilities.cpp)
target_include_directories(macmst_displayport PUBLIC src)
target_link_libraries(macmst_displayport PUBLIC macmst_options)
if(BUILD_TESTING)
    add_executable(macmst_dpcd_tests tests/dpcd_tests.cpp)
    target_link_libraries(macmst_dpcd_tests PRIVATE macmst_displayport)
    add_test(NAME dpcd_capabilities COMMAND macmst_dpcd_tests)
    set_tests_properties(dpcd_capabilities PROPERTIES LABELS unit)
endif()

if(APPLE)
    enable_language(OBJCXX)
    set(CMAKE_OBJCXX_STANDARD 20)
    set(CMAKE_OBJCXX_STANDARD_REQUIRED ON)
    set(CMAKE_OBJCXX_EXTENSIONS OFF)
    find_library(FOUNDATION_FRAMEWORK Foundation REQUIRED)
    find_library(COREGRAPHICS_FRAMEWORK CoreGraphics REQUIRED)
    find_library(IOKIT_FRAMEWORK IOKit REQUIRED)

    add_library(macmst_macos src/platform/macos/probe.mm)
    target_include_directories(macmst_macos PUBLIC src)
    target_compile_options(macmst_macos PRIVATE -fobjc-arc)
    target_link_libraries(macmst_macos PUBLIC macmst_options
        ${FOUNDATION_FRAMEWORK} ${COREGRAPHICS_FRAMEWORK} ${IOKIT_FRAMEWORK})

    add_executable(macmst src/cli/main.cpp)
    target_link_libraries(macmst PRIVATE macmst_macos)
    if(BUILD_TESTING OR MACMST_ENABLE_DPDV_OPEN_EXPERIMENT)
        add_library(macmst_mock_isolation src/isolation/mock_helper.cpp)
        target_include_directories(macmst_mock_isolation PUBLIC src)
        target_link_libraries(macmst_mock_isolation PUBLIC macmst_options)
    endif()
    if(MACMST_ENABLE_DPDV_OPEN_EXPERIMENT)
        find_library(COREFOUNDATION_FRAMEWORK CoreFoundation REQUIRED)
        add_executable(macmst_dpdv_open_helper src/isolation/dpdv_open_helper.cpp)
        target_include_directories(macmst_dpdv_open_helper PRIVATE src)
        target_link_libraries(macmst_dpdv_open_helper PRIVATE macmst_options ${COREFOUNDATION_FRAMEWORK} ${IOKIT_FRAMEWORK})
        add_executable(macmst_dpdv_open_check src/isolation/dpdv_parent.cpp)
        target_link_libraries(macmst_dpdv_open_check PRIVATE macmst_mock_isolation)
    endif()
    if(BUILD_TESTING)
        add_executable(macmst_probe_tests tests/probe_tests.mm)
        target_compile_options(macmst_probe_tests PRIVATE -fobjc-arc)
        target_link_libraries(macmst_probe_tests PRIVATE macmst_macos)
        add_test(NAME probe_privacy COMMAND macmst_probe_tests)
        set_tests_properties(probe_privacy PROPERTIES LABELS unit)
        add_executable(macmst_mock_helper tests/mock_helper.cpp)
        target_include_directories(macmst_mock_helper PRIVATE src)
        target_link_libraries(macmst_mock_helper PRIVATE macmst_options)
        add_executable(macmst_isolation_tests tests/isolation_tests.cpp)
        target_link_libraries(macmst_isolation_tests PRIVATE macmst_mock_isolation)
        add_test(NAME mock_helper_isolation
            COMMAND macmst_isolation_tests $<TARGET_FILE:macmst_mock_helper>)
        set_tests_properties(mock_helper_isolation PROPERTIES LABELS "unit;mock;integration" TIMEOUT 30)
        add_test(NAME cli_help COMMAND macmst --help)
        add_test(NAME cli_reject_unknown COMMAND macmst enable-mst)
        set_tests_properties(cli_reject_unknown PROPERTIES WILL_FAIL TRUE)
        set_tests_properties(cli_help cli_reject_unknown PROPERTIES LABELS unit)
    endif()
endif()

if(BUILD_TESTING)
    find_package(Python3 COMPONENTS Interpreter REQUIRED)
    add_test(NAME capture_privacy
        COMMAND ${Python3_EXECUTABLE} -m unittest discover -s tests -p test_capture_baseline.py -v)
    set_tests_properties(capture_privacy PROPERTIES WORKING_DIRECTORY ${PROJECT_SOURCE_DIR} LABELS unit)
    add_test(NAME dcp_trace_import
        COMMAND ${Python3_EXECUTABLE} -X dev -W error -m unittest discover -s tests -p "test_dcp_trace*.py" -v)
    set_tests_properties(dcp_trace_import PROPERTIES WORKING_DIRECTORY ${PROJECT_SOURCE_DIR} LABELS "unit;offline")
    add_test(NAME host_stream_control
        COMMAND ${Python3_EXECUTABLE} -X dev -W error -m unittest discover -s tests -p test_host_stream_control.py -v)
    set_tests_properties(host_stream_control PROPERTIES WORKING_DIRECTORY ${PROJECT_SOURCE_DIR} LABELS "unit;offline")
    add_test(NAME runtime_display_snapshot
        COMMAND ${Python3_EXECUTABLE} -X dev -W error -m unittest discover -s tests -p test_runtime_display_snapshot.py -v)
    set_tests_properties(runtime_display_snapshot PROPERTIES WORKING_DIRECTORY ${PROJECT_SOURCE_DIR} LABELS "unit;offline")
    add_test(NAME display_log_analyze
        COMMAND ${Python3_EXECUTABLE} -X dev -W error -m unittest discover -s tests -p test_display_log_analyze.py -v)
    set_tests_properties(display_log_analyze PROPERTIES WORKING_DIRECTORY ${PROJECT_SOURCE_DIR} LABELS "unit;offline")
    add_test(NAME dp_wire
        COMMAND ${Python3_EXECUTABLE} -X dev -W error -m unittest discover -s tests -p test_dp_wire.py -v)
    set_tests_properties(dp_wire PROPERTIES WORKING_DIRECTORY ${PROJECT_SOURCE_DIR} LABELS "unit;offline")
    add_test(NAME dp_aux_frontend
        COMMAND ${Python3_EXECUTABLE} -X dev -W error -m unittest discover -s tests -p test_dp_aux_frontend.py -v)
    set_tests_properties(dp_aux_frontend PROPERTIES WORKING_DIRECTORY ${PROJECT_SOURCE_DIR} LABELS "unit;offline")
    add_test(NAME dp_aux_closure
        COMMAND ${Python3_EXECUTABLE} -X dev -W error -m unittest discover -s tests -p test_dp_aux_closure.py -v)
    set_tests_properties(dp_aux_closure PROPERTIES WORKING_DIRECTORY ${PROJECT_SOURCE_DIR} LABELS "unit;offline")
    add_test(NAME dp_aux_differential
        COMMAND ${Python3_EXECUTABLE} -X dev -W error -m unittest discover -s tests -p test_dp_aux_differential.py -v)
    set_tests_properties(dp_aux_differential PROPERTIES WORKING_DIRECTORY ${PROJECT_SOURCE_DIR} LABELS "unit;offline")
    add_test(NAME dp_aux_receiver
        COMMAND ${Python3_EXECUTABLE} -X dev -W error -m unittest discover -s tests -p test_dp_aux_receiver.py -v)
    set_tests_properties(dp_aux_receiver PROPERTIES WORKING_DIRECTORY ${PROJECT_SOURCE_DIR} LABELS "unit;offline")
    add_test(NAME iodp_static_parsers
        COMMAND ${Python3_EXECUTABLE} -m unittest discover -s tests -p test_iodp_static.py -v)
    set_tests_properties(iodp_static_parsers PROPERTIES WORKING_DIRECTORY ${PROJECT_SOURCE_DIR} LABELS unit)
    if(APPLE)
        add_test(NAME cli_contract
            COMMAND ${Python3_EXECUTABLE} ${PROJECT_SOURCE_DIR}/tests/test_probe_cli.py $<TARGET_FILE:macmst>
                --mock-executables $<TARGET_FILE:macmst_mock_helper> $<TARGET_FILE:macmst_isolation_tests>)
        set_tests_properties(cli_contract PROPERTIES LABELS unit)
        if(MACMST_ENABLE_DPDV_OPEN_EXPERIMENT)
            add_test(NAME dpdv_contract
                COMMAND ${Python3_EXECUTABLE} ${PROJECT_SOURCE_DIR}/tests/test_dpdv_open_check.py
                    $<TARGET_FILE:macmst> $<TARGET_FILE:macmst_dpdv_open_check> $<TARGET_FILE:macmst_dpdv_open_helper>)
            set_tests_properties(dpdv_contract PROPERTIES LABELS unit)
        endif()
        if(MACMST_ENABLE_HARDWARE_TESTS)
            add_test(NAME probe_hardware
                COMMAND ${Python3_EXECUTABLE} ${PROJECT_SOURCE_DIR}/tests/test_probe_cli.py $<TARGET_FILE:macmst> --hardware)
            set_tests_properties(probe_hardware PROPERTIES LABELS hardware)
        endif()
    endif()
endif()